<UserControl x:Class="LoLHelpAplication.View.ItemViews"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    
    
    <Grid Margin="0,0,-203,-133">

        <Image Source="{Binding DisplayedImage}" HorizontalAlignment="Left" Height="118" Margin="68,34,0,0" VerticalAlignment="Top" Width="142"/>
        <Label Content="{Binding Name}" HorizontalAlignment="Left" Margin="240,34,0,0" VerticalAlignment="Top" Width="242"/>
        <Label Content="{Binding Total}" HorizontalAlignment="Left" Margin="240,65,0,0" VerticalAlignment="Top" Width="242"/>
        <Label Content="{Binding Sell}" HorizontalAlignment="Left" Margin="240,96,0,0" VerticalAlignment="Top" Width="242"/>
        <TextBox HorizontalAlignment="Left" Height="240" Margin="10,174,0,0" TextWrapping="Wrap" Text="{Binding Descriptions}" VerticalAlignment="Top" Width="472" IsEnabled="False" ScrollViewer.CanContentScroll="True"/>

    </Grid>
</UserControl>
